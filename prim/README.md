# prim
₱4 == four, forever.
```
USAGE:
    prim <QUERY>

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

ARGS:
    <QUERY>    a word of length < 999
```
